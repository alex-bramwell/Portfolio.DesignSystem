/** @type {import('next').NextConfig} */
const nextConfig = {
  basePath: '/Portfolio.DesignSystem',
  assetPrefix: '/Portfolio.DesignSystem/',
  images: { unoptimized: true },
  output: 'export'
}
module.exports = nextConfig